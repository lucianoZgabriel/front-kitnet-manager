/**
 * Re-exportando o hook useAuth do contexto de autenticação
 * Isso permite importar de um local mais semântico:
 * import { useAuth } from '@/src/hooks/use-auth'
 */

export { useAuth } from '@/src/contexts/auth-context'
